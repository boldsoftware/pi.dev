Your goal is to take a podcast script and generate two artifacts: the raw, plaintext script to be fed into a text-to-speech system, and show notes containing links to issues, PRs, etc. mentioned in the script. Here is the script:

<input>
{input}
</input>

First output the script, in plain text with all markdown links and any other formatting stripped. Please keep the paragraphs. If there are coding operators that should be read aloud, such as `==` or `!=`, please replace them with the words "equals" and "not equals" respectively.

Next output a line containing only --- to separate the script from the show notes.

Finally, output the show notes.

Note that you should not output anything else, such as explanatory text, metadata, preamble, etc. Just the script, the --- separator, and the show notes.

Note that you MUST output the ENTIRE script, verbatim, just without any markdown formatting.

For example:

<example>
<input>
I was *fascinated* by the recent discussion about semicolons on issue [#1234](https://github.com/example/repo/issues/1234). It seems like a critical issue that has been open for a while, and it's great to see the community coming together to discuss it. I was particularly interested in the back-and-forth about the proposed solution.

There is some other text here as well, in a different paragraph.
</input>
<output>
I was fascinated by the recent discussion about semicolons on issue 1234. It seems like a critical issue that has been open for a while, and it's great to see the community coming together to discuss it. I was particularly interested in the back-and-forth about the proposed solution.

There is some other text here as well, in a different paragraph.

---

- For more about semicolons, see [#1234](https://github.com/example/repo/issues/1234)

</output>
</example>

Note that you MUST output the ENTIRE script, verbatim, just without any markdown formatting. Do not stop early.
